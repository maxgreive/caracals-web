# caracals-web

Landing page built with Astro and the Radiant theme.

## Development

```sh
pnpm install
pnpm dev
```

Create a production build with `pnpm build` and preview it with `pnpm preview`.

Content is managed through Decap CMS at `/admin/`. Training status is stored in
SheetDB and edited through the no-index `/trainer` page.
