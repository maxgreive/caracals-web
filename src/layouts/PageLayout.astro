---
import MainLayout from "./MainLayout.astro";
import ProjectHeader from "../components/ProjectHeader.astro";
const { frontmatter } = Astro.props;
---

<MainLayout>
  {frontmatter.featuredImage && <ProjectHeader frontmatter={frontmatter} />}
  <slot />
</MainLayout>
